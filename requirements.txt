torch>=2.1.0
numpy>=1.24.0

# Optional, for faster/production-grade voxel downsampling and real
# dataset preprocessing (Toronto-3D .ply, point cloud viz, KD-tree label
# transfer). Not required to run main.py with synthetic data.
open3d>=0.18.0
scipy>=1.11.0
